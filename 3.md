Enhance the `improve` operation's feedback in SensorFlow: (C3) show prior dead-end
attempts on the same parent so the LLM stops rediscovering them, and (I1) add an
epsilon-constraint instruction so it stops trading away other objectives. Both are
additive.

Read the SensorFlow Project Context Primer first, then READ THE ACTUAL CODE and fit your
implementation to its existing patterns — the builder's signature, how feedback strings
are currently assembled, how the archive is accessed, naming. Adapt freely; the structure
below is intent, not a spec to transcribe. Follow the primer's conventions (additive,
simplest thing that works, no new deps/abstractions, shared _rng, don't rename keys).

GOAL
- C3: When building improve feedback, gather the parent's sibling attempts (same parent,
  earlier tries) and surface the DEAD ENDS — ones the parent dominates, and ones that
  errored — so the LLM sees what's already been tried and failed. Concretely this must
  stop the observed failure mode where improve re-finds the parent's quality at worse size
  (matched accuracy/f1, larger ROM) over and over.
- I1: Instruct the model to improve the target metric WITHOUT regressing the parent's other
  objectives, and tell it that a candidate the parent dominates is a failure, not progress.

NON-NEGOTIABLE (these break things if improvised wrong — don't deviate):
1. Isolation. Touch ONLY the improve feedback path. Do not modify choose_operation, parent
   selection, or the repair/combine builders — they're separate experiments and this must
   be attributable alone. Threading new params into the improve builder is the one allowed
   plumbing change.
2. EvaluationResult is an object: .status / .metrics / .failure_reason. Not dict access.
3. Read the LIVE metric policy from the code (objectives + directions). Never hard-code or
   assume the keys — they've changed before.
4. Dead-end rows must show the FULL metric vector, not a single score. The whole point is
   visible multi-objective trade-offs ("same quality, worse ROM") — collapsing to one
   number destroys the signal.
5. I1 default is STRICT no-regression. If you add tolerance it must be PER-OBJECTIVE
   (accuracy ~0..1 and ROM in bytes can't share one epsilon).
6. Determinism: any cap/truncation is by try_id, not sampling. No new RNG.

YOUR DISCRETION (fit to the codebase):
- How you collect and dominance-check siblings; where the parent's try_id comes from.
- The rendered format. Prefer a compact structured block over prose (more reliable for the
  target model), but pick whatever fits the existing feedback style and reads cleanly. Cap
  length sensibly.
- Whether a per-row "what changed" description is feasible: use it ONLY if the archive
  already stores one. If not, go metrics-only and note that a change summary would need a
  stored description — do NOT build a diff-summarizer in the loop.
- Exact wording of the I1 instruction, matching the existing prompt's voice.

TEST (no Bedrock spend):
Assemble and PRINT the improve feedback for one real parent that has dead-end siblings
(parent 0 is a good pick). Do not call mutate(). Show me the output and STOP for review.

If anything in the code contradicts the above, STOP and report rather than adapting around
it.
